services:
  auto-claimer:
    build: .
    env_file: .env
    volumes:
      - ./src:/app/src
    command: yarn auto-claimer

  cli:
    build: .
    env_file: .env
    volumes:
      - ./src:/app/src
    entrypoint: ["yarn", "cli"]
    profiles: ["cli"]